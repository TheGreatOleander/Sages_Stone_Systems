# Identity System (Gold Standard)

This system defines and validates identities within Sages Stone.

## Invariants
- Every identity has an issuer and a subject.
- Identities are immutable once created.
- Invalid identities fail validation immediately.

## Flow
schema -> rules -> executor

This system conforms to the Contract System gold standard.\n